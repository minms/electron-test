language: node_js
node_js: stable

cache:
  directories:
  - node_modules
  - $HOME/.cache/electron
  - $HOME/.cache/electron-builder

install:
  - yarn install

script:
  - yarn build:win32
branches:
  except:
    - "master"